/**
 * Time window for an assigned driver to accept after vendor accepts the order.
 * Currently 5 minutes; adjust here if product requirements change.
 */
export const DRIVER_ASSIGNMENT_WINDOW_MS = 5 * 60 * 1000;
